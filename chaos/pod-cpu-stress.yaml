apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: pagamento-pod-cpu-stress
  namespace: app
  labels:
    app.kubernetes.io/name: pagamento
    chaos-test: "true"
spec:
  mode: one
  selector:
    namespaces:
      - app
    labelSelectors:
      app: pagamento
  stressors:
    cpu:
      workers: 1
      load: 80
  duration: "60s"
  scheduler:
    cron: "@every 10m"
